export default function ProfileFormLayout({children}: {children: React.ReactNode;}) {
  return (
      
        <div className="flex items-center justify-center h-full bg-neutral-1000 p-4"> 
          {children}
        </div>
      
  );
}
